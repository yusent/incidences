en:
  activerecord:
    errors:
      models:
        incidence: &shared_msgs
          attributes:
            start_date:
              invalid: "should not be after end_date"
        payroll_invoice:
          <<: *shared_msgs
